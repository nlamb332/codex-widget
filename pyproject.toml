[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-usage-rings"
version = "0.1.0"
description = "Standalone Windows desktop app for Codex usage rings"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.6",
]

[project.scripts]
codex-usage-rings = "codex_usage_rings.app:main"

[tool.setuptools.packages.find]
where = ["src"]
